import React from "react";

const BlogHome = ({}) => {
  return <div>
      Hello world
  </div>;
};

BlogHome.getAnimation = () => {
  return ["hide", "offset", "rest"];
};

export default BlogHome;
